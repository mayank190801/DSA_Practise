public class june24que4 {
    public static void main(String[] args) {

    }

    // 12-2 (placement prep time for sure - no matter what)
    public static int[] minOperations(String boxes) {
        char[] str = boxes.toCharArray();
        int len = str.length;
        int[] ans = new int[len];

        int count = 0;
        int[] temp = new int[len];
        for (int i = 0; i < len; i++) {
            // some logic would go here, and then else below once again,
            // first focus on solve thing problem the fastest, and implementing that
            // solution
            // then later focus on basically other stuff, that is optimizing and what not
            // that is what I do currently as of now, maybe in future I might be doing
            // things differently, who knows
            // but this field compounds for sure -- no matter what, the focus time is insane

        }

    }

}

// made changes